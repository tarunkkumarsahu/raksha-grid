# Android App

The first Android shell is written in **Kotlin + Jetpack Compose** and contains role-specific Citizen, Responder, and Officer demo views.

Open the mobile folder in Android Studio, sync Gradle, then run the app configuration on an emulator or Android device.

This milestone deliberately uses labelled simulation data. API wiring, maps, GPS, photo reporting, offline caching, and navigation are next.
